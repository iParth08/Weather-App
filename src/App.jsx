import { useState } from "react";
import "./App.css";

function App() {
  const [count, setCount] = useState(0);

  return <div className="text-xl font-semibold text-center">
    This is a clean template for Vite + React
    </div>;
}

export default App;
